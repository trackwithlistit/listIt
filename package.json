{
  "name": "listit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"const fs=require('fs'); try { fs.cpSync('dist', '../dist', {recursive: true}); } catch(e){}\""
  }
}
